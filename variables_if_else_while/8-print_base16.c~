#include <stdio.h>

/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: Always 0.
 */
int main(void)
{
int x;
char y;
for (x = 0; x < 10; x++)
putchar(x);
for (y = a; y = f; y++)
putchar(y);
return (0);
}
