#include <stdio.h>
/**
 *main - esta funcion es para escribir el comentario en puts
 *Return: el valor de error de la funcion
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
