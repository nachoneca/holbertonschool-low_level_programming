#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
*_isalpha - devuelve si es mayuscula o no
*@c: parametro letras
*Return: 0 o 1 segun lo que sea
*/
int _abs(int n)
{
int num = abs(n);
return(num);
}
