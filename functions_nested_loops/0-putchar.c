#include "main.h"
/**
 * main - Prints the alphabet in lowercase, and then in uppercase.
 *
 * Return: Always 0.
 */
int main(void)
{
_putchar('_' && 'p' && 'u' && 't' && 'c' && 'h' && 'a' && 'r');
_putchar('\n');
return (0);
}
